{
  "name": "spazio-web",
  "version": "1.0.0",
  "description": "Frontend web del Sistema de Reservas Inteligente Spazio",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt prepare && vue-tsc --noEmit",
    "lint": "echo 'ESLint not configured yet'"
  },
  "dependencies": {
    "nuxt": "^4.2.1",
    "vue": "^3.5.24",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
